\documentclass[12pt]{article}
\usepackage[dvips,letterpaper,margin=1in,bottom=0.7in]{geometry} 
\usepackage{amsmath} % AMS Math Package
\usepackage{amsthm} % Theorem Formatting
\usepackage{amssymb}    % Math symbols such as \mathbb
\usepackage{graphicx, tikz} % Allows for eps, tikz images 
\usepackage{enumerate}
\usepackage{array}

% Allows for importation of mathcha.io tikz code
\usetikzlibrary{fadings}
\usetikzlibrary{patterns}
\usetikzlibrary{shadows.blur}
\usetikzlibrary{shapes}

% For figures
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}

% Basic page formatting
\usepackage{fancyhdr}

% Define colored boxes for problem statements
\usepackage[many]{tcolorbox}

% Define any color you want
% \definecolor{myblue}{RGB}{0,163,243}

% Just replace gray with predefined color name if you wish
%-----------------------------------------------------------%
\tcbset{mystyle/.style={
  breakable,
  enhanced,
  outer arc=0pt,
  arc=0pt,
  colframe=gray,
  colback=gray!20,
  attach boxed title to top left,
  boxed title style={
    colback=gray,
    outer arc=0pt,
    arc=0pt,
    },
  title=Problem~\thetcbcounter,
  fonttitle=\rmfamily
  }
}

\newtcolorbox[auto counter]{problem}[1]{
  mystyle,
  title=Problem~\thetcbcounter
  }
%---------------------------------------------------------%

% Predefine solution environment
\newenvironment{solution}{%\small%
        \begin{trivlist} \item \textbf{Solution}. }{%
            \hspace*{\fill} $\blacksquare$\end{trivlist}}%

%Basic page formatting
\pagestyle{fancy}
\fancyhf{}
\rhead{Subject}
\lhead{\textbf{Last Name, First Name, M.I.}}
\chead{PS no.}
\rfoot{\thepage}
%---------------------------------------------------------%

% Pertinent electrodynamics vectors
\newcommand{\uvec}[1]{\boldsymbol{\hat{#1}}} %unit boldface vectors
\newcommand{\bvec}[1]{\boldsymbol{#1}} %boldface vectors
\def\rcurs{{\mbox{$\resizebox{.09in}{.08in}{\includegraphics[trim= 1em 0 14em 0,clip]{ScriptR.pdf}}$}}} %griffith separation vector distance
\def\brcurs{{\mbox{$\resizebox{.09in}{.08in}{\includegraphics[trim= 1em 0 14em 0,clip]{BoldR.pdf}}$}}} %griffith separation vector 
\def\hrcurs{{\mbox{$\hat \brcurs$}}} %griffith unit separation vector 

%use this command when showing elementary row operations
%\stackrel[-5R_1 + 2R_2 \to R_2']{3R_1 + 2R_3 \to R_3'}{\xrightarrow{\hspace*{2cm}}}

%Note: Always add an escape character \ after problem environment. 

\begin{document}
\begin{problem} 
    \ Insert Problem 1 statement here 
\end{problem}
\begin{solution}
    Solution here.
\end{solution}

\begin{problem} 
    \ Insert Problem 2 statement here 
\end{problem}
\begin{solution}
    Solution here.
\end{solution}

\end{document}